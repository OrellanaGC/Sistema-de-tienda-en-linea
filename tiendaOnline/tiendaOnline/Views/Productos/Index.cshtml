@using tiendaOnline.Data
@model IEnumerable<tiendaOnline.Models.Producto>
@inject ApplicationDbContext _context
@{
    ViewData["Title"] = "Productos";
}

<h2>Index</h2>
<p>
    <a asp-action="Create">Create New</a>
</p>
<!--table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Existencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetalleProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subcategoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreProducto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Precio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Existencia)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Codigo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Imagen)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DetalleProducto.DetalleProductoID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Subcategoria.SubcategoriaID)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ProductoID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ProductoID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ProductoID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table-->
<div class="row">
    <div>
        @foreach (var producto in Model)
        {
            <div class="col-md-3">
                <img class="zoom" height=260px width=260px src="~/images/productos/@Html.DisplayFor(modelItem => producto.Imagen)" />
                <h4>@Html.DisplayFor(modelItem => producto.Precio)</h4>
                <a asp-action="Details" asp-route-id="@producto.ProductoID">@Html.DisplayFor(modelItem => producto.NombreProducto)</a>
                <div class="agregarCarrito text-left">
                    <p class="button">
                        <a class="btn btn-success" id="cartButton"
                           asp-controller="Carrito"
                           asp-action="AgregarCarrito"
                           asp-route-idProducto="@producto.ProductoID">
                            Añadir al carrito
                        </a>
                    </p>

                </div>
            </div>
        }
    </div>
</div>
