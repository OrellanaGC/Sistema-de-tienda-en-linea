@model IEnumerable<tiendaOnline.Models.TipoDePago>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Paypal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarjeta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Paypal.PaypalID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarjeta.TarjetaID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoDePagoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoDePagoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoDePagoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
