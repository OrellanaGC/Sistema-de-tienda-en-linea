@model IEnumerable<tiendaOnline.Models.Municipio>

@{
    ViewData["Title"] = "Municipios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Municipios</h2>

<p>
    <a class="btn btn-info" asp-action="Create"><i class="fas fa-plus"></i> Agregar </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreMunicipio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreMunicipio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departamento.nombreDepartamento)
            </td>
            <td>
                <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.MunicipioID"><i class="fa fa-pen"></i></a>
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.MunicipioID"><i class="fa fa-info"></i></a>
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.MunicipioID"><i class="fa fa-trash-alt"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
