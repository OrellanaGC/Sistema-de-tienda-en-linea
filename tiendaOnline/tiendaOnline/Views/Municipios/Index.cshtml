@model IEnumerable<tiendaOnline.Models.Municipio>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreMunicipio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreMunicipio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departamento.nombreDepartamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MunicipioID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MunicipioID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MunicipioID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
