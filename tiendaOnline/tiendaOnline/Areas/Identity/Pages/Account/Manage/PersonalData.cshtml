@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Tu cuenta tiene la información que no has proporcionado. Esta página te permite descargarla o borrarla.</p>
        <p>
            <strong> Borrando esta información, eliminara permanentemente tu cuenta, y no puede ser revertido.</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-default" type="submit">Descargar</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-default">Borrar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}